const path = require('path');
const fs = require('fs');

const p = path.join(
    process.require.main.filename
)

class Card {
    add() {
        
    }

    static async feth() {

    }
}

module.exports = Card;